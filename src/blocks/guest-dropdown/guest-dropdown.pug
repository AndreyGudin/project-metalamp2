mixin guest-dropdown(options)
  - var i=1
  - var mas=[...options.menuItems]
  div(class=`${options.className}-block`)
  label(class=`${options.className}__title`)=options.title
  div(class = `${options.className} iqdropdown`)
    p.iqdropdown-selection
    div(class = `${options.className} iqdropdown-menu`, data-hide = options.hide)
      each item in options.menuItems
        .iqdropdown-menu-option(data-id=`item${i}`)
          div
            p.iqdropdown-item=item
            - i++
//-mixin guest-dropdown(flabel,slabel,tlabel,cls,dtitle,hide)
  -var i = cls + ' iqdropdown'
  -var ii = cls + ' iqdropdown-menu'
  div(class=cls + "-block")
    label(class=cls + "__title")=dtitle
    div(class = i)
      p.iqdropdown-selection
      div(class = ii, data-hide = hide) 
        .iqdropdown-menu-option(data-id="item1")
          div
            p.iqdropdown-item=flabel
        .iqdropdown-menu-option(data-id="item2")
          div
            p.iqdropdown-item=slabel
        .iqdropdown-menu-option(data-id="item3")
          div
            p.iqdropdown-item=tlabel